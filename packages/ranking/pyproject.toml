[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-math-ranking"
version = "0.1.0"
description = "AI 数学抽卡系统 · 基线排序（版本化权重）"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
